[project]
name = "controle-estoque"
version = "1.0.0"
description = "Aplicação desktop em Python para controle de estoque básico"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100